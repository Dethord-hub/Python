import random


digits = '0123456789'
low_let = 'abcdefghijklmnopqrstuvwxyz'
upp_let = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'
punctuation = '!#$%&*+-=?@^_'


def greet_human():
    print("Приветствую, кожанный! Как тебя зовут?")
    print(f"{input()}? Интересно, интересно...")
    print("Что же, это все равно неважно, кусок мяса, ты зашел сюда, чтобы сгенерировать пароль?")
    if input().lower() in ['да', 'lf', 'yes', 'y', 'д']:
        password_options()
    else:
        print("Серьезно, тогда \n")
        print("  O/ \n /|  \n / \\   Пошел нахуй! 😜\n")


def password_options():
    char = ''
    count_pass = int(input("Сколько паролей мне сгенерировать?\n"))
    len_pass = int(input("Какой длины пароль генерировать?\n"))
    pwd_digits = input('Включать ли в пароль цифры от 0 до 9?\n')
    pwd_uppercase = input('Включать ли в пароль прописные буквы?\n')
    pwd_lowercase = input('Включать ли в пароль строчные буквы?\n')
    pwd_punctuation = input('Включать ли в пароль символы "!#$%&*+-=?@^_"?\n')
    pwd_exceptions = input('Исключать ли неоднозначные символы "il1Lo0O"?\n')

    if pwd_digits.lower() in ['да', 'lf', 'yes', 'y', 'д']:
        char += digits
    if pwd_uppercase.lower() in ['да', 'lf', 'yes', 'y', 'д']:
        char += upp_let
    if pwd_lowercase.lower() in ['да', 'lf', 'yes', 'y', 'д']:
        char += low_let
    if pwd_punctuation.lower() in ['да', 'lf', 'yes', 'y', 'д']:
        char += punctuation
    if pwd_exceptions.lower() in ['да', 'lf', 'yes', 'y', 'д']:
        for i in chars:
            if i not in ['i','l','1','L','o','0','O']:
                char += i


    print(char)
    generate_password(count_pass, len_pass, char)
    return

def generate_password(count,len,char):
    for _ in range(count):
        print()
        for j in range(len):
            print(random.choice(char), end='')
    print()



if __name__ == "__main__":
    greet_human()
